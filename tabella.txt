MATERIE:
id (INT UNSIGNED PRIMARY KEY AUTO_INCREMENT)
nome (VARCHAR(50) NOT NULL UNIQUE)
durata(TINYINT UNSIGNED)
cfu(TINYINT)
propedeutico(TINYINT DEFAULT(0))
id_corso_di_laurea (INT UNSIGNED NOT NULL, FOREIGN KEY REFERENCES Corsi_di_Laurea(id))

INSEGNANTI:
id (INT UNSIGNED PRIMARY KEY AUTO_INCREMENT)
nome (VARCHAR(50) NOT NULL)
cognome (VARCHAR(50) NOT NULL)
telefono (VARCHAR(10) NOT NULL)
data_di_nascita (VARCHAR(10) NOT NULL)

STUDENTI:
id (INT UNSIGNED PRIMARY KEY AUTO_INCREMENT)
nome (VARCHAR(50) NOT NULL)
cognome (VARCHAR(50) NOT NULL)
email (VARCHAR(50) NOT NULL)
indirizzo (VARCHAR(50) NOT NULL)
id_corso_di_laurea (INT UNSIGNED NOT NULL, FOREIGN KEY REFERENCES Corsi_di_Laurea(id))

APPELLI_D_ESAME:
id (INT UNSIGNED PRIMARY KEY AUTO_INCREMENT)
data_ora(DATETIME)
nome(VARCHAR(50) NOTNULL)
modalità(VARCHAR(50))
luogo(VARCHAR(50))
id_materie (INT UNSIGNED NOT NULL, FOREIGN KEY REFERENCES Corsi(id))
id_insegnante (INT UNSIGNED NOT NULL, FOREIGN KEY REFERENCES Insegnanti(id))
id_studente (INT UNSIGNED NOT NULL, FOREIGN KEY REFERENCES Studenti(id))

TABELLE PONTE----------------------------------------------------------------------------------------------------------------

STUDENTI_APPELLI:
voto (TINYINT UNSIGNED NOT NULL)
id_studente (INT UNSIGNED NOT NULL, PRIMARY KEY FOREIGN KEY REFERENCES Studenti(id))
id_appelli (INT UNSIGNED NOT NULL, PRIMARY KEY FOREIGN KEY REFERENCES APPELLI_D_ESAME(id))

MATERIE_INSEGNANTI:
id_materie (INT UNSIGNED NOT NULL, PRIMARY KEY FOREIGN KEY REFERENCES MATERIE(id))
id_insegnanti (INT UNSIGNED NOT NULL, PRIMARY KEY FOREIGN KEY REFERENCES INSEGNANTI(id))